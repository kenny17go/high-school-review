# V4.9.7.7 高二段考範圍智慧分流版

基準：main 4ae4232；保留既有 V4.9.7.7 未提交成果，增量補齊相容性。本機成果已提交保存，經使用者核准合併遠端 a7c69fe 後發布；未修改正式資料庫。

## 共用資料結構

learning-catalog.js 定義科目、課程、章節、範圍與雲端 adapters。完整鍵為 subjectId / courseId / grade / school / academic_year / semester / exam。academic_year 明確為 112、113、114，保留 academicYear 相容別名；共有 8 校 × 3 年 × 3 分流 × 2 學期 × 3 段考 = 432 筆平台紀錄，各年可獨立覆蓋。不存在跨年度的 null 預設。110、111 尚未建立高二 mapping 時，顯示待建立且不抽取其他年度題目。

數 A 可用 A+AB；數 B 可用 B+AB；「全部（AB 共通）」只用 AB。既有 chapter ID 保留，課程適用性由 curriculumTrack 獨立判定；歷史 ID 的 a 前綴不代表所有題目只適用 A。

grade2-questions.js 保留 420 題與原始 ID／題意，來自 21 個參數化模板各 20 題，不宣稱有 420 個獨立題型。分類為 A 專屬 140、AB 共通 280、B 專屬 0。原有空間距離 20 題歸入空間坐標，避免誤認成進階空間向量。高一 fallback 與 extra 原檔未修改，合計 250 題。

## 平台計畫

各校目前共用平台計畫，但具有獨立學年度與分流紀錄；不捏造學校進度差异。

| 分流 | 上一／上二／上三 | 下一／下二／下三 |
| --- | --- | --- |
| A | 三角函數／指數與對數／平面向量 | 空間向量與坐標／空間平面與直線／機率與矩陣 |
| B、AB | 三角函數／指數與對數／平面向量 | 空間坐標／矩陣／機率 |

## 已收錄來源核對

grade2-scopes.js 保留來源 URL、reviewedAt、學年度、班群與細項限制。

- 建國中學 113 上學期一段：官方數 A 三角函數，適用 201–223 班，225–228 班另訂。數 B 僅 203 班選修，弧度量、週期模型、指數函數與圖形；使用 allowedSubtopics 白名單，不包含整章對數。共 2 筆可用官方映射。
- 師大附中 113 下學期一段：已核對 A「第四冊 1-1 到 2-1（部分），P1–P77」、B「Ch1 全」，教材版本與部分邊界未明，保留 pending，使用平台標籤。
- 中山女高 114 下學期三段：已核對 A「第四章」、B「第 6、7 單元」，缺章名／教材版次，保留 pending。
- 以上後兩校共 4 筆待精確映射；其餘組合為平台模擬。本次沒有新增推定範圍；來源索引存在不代表已有可安全抽題的範圍。

## 抽題與相容性

練習、模考皆先解析同一 currentScope，再經 getSchoolPool / catalog.pool 過濾科目、年級、分流、學期、章節、跨章依賴與細項白名單。雲端與符合相同範圍的本機题合併去重，數量不足只提供實際數量，不擴大範圍。題組保存範圍快照；更換條件或本機模式使舊非同步請求失效。

每份高二題組複製並隨機排列選項，同步正確答案索引；optionOrder 保留原始索引，雲端 attempts 寫入還原後的 selected_index。負數本機題 ID 不送入 SQL 外鍵。原始題庫不受隨機化污染。

Supabase questions 的既有 subject_id 透過 subjects.id/code=MATH 解析，不硬編碼科目 ID。curriculum_track 支援 A/B/AB；缺 semester/chapter_id 時，只用已審核章節映射。「空間與矩陣」依 subtopic 拆分，未知內容拒收，不混入高一。既有 scope rows 缺分流或來源證據時不假裝是高二官方映射；本機 reviewed scopes 補足已核實來源。沒有新增欄位或 migration。

保留 v42_url / v42_key；版本更新與本機模式不清除設定。保留題源、學測、資料完整度、學習數據與我不會等入口。

## 驗證

npm test 執行 scripts/validate.mjs 與 tests/browser.test.cjs：
- JS 語法、HTML 結構／唯一 ID、版本與設定保護檢查。
- 432 組分流、420 題獨立計算答案驗證、250 題高一、跨年級／分流／學期排除、跨年度覆蓋。
- 真實 DOM 高一／高二練習與模考、432 組相同 mapping、隨機化後評分、我不會／錯題／詳解／統計、來源入口、資料完整度、設定保留、390px 排版。
- 隔離雲端 fixture 模擬既有 schema、原始索引同步、延遲回應、學校保留與學測年份限制。

tests/live-readonly.mjs 透過唯讀 REST GET 驗證正式 Supabase，2026-09-13 結果：8 校、高一 250、高二 200，200 題全部可適配；A 85、B 19、AB 96，分流可用 A181 / B115 / AB96。正式 exam_scope_profiles 高二紀錄目前為 0。與本機題合併後 432 組全部通過範圍檢查。驗證未写入 attempts 或其他正式表。

登入信件、正式使用者寫入流程及實機 iPhone/PWA 安裝未做端對端驗證；瀏覽器使用隔離帳號 fixture。既有 manifest、圖示與 config／高一題庫不變。questions 原有 1000 筆載入上限保留，未來擴全科目須另加分頁或課程查詢。

## 保留的既有修正

松山 fallback 學校合併、重複 DOM ID 修正、currentUser / updateStats 未定義引用修正、我不會錯題相容儲存、題目文字 HTML 跳脫及來源篩選 listener 均保留。題庫資料工程不額外顯示版本號。

## 發布整合
遠端新增的歷史題庫與說明檔完整保留，資料完整度未公開狀態已整合。舊高二檔會把題目加入高一 fallback，故不直接掛載；本版使用已驗證的獨立 420 題池和雲端 200 題 adapter。
